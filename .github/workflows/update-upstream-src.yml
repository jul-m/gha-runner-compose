name: Update Upstream Sources
run-name: "Update Upstream Sources - ${{ github.event_name }} - #${{ github.run_number }}"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'  # Run daily

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  # Independent of update-runner-images-src (no needs:) so a submodule-sync
  # failure can never block cleanup, and vice versa - same isolation
  # principle as splitting ci-large-tiers.yml out of ci.yml. Runs once daily
  # via this workflow's own cron instead of from every ci.yml/ci-large-tiers.yml
  # run, since staging/cache pruning doesn't need to happen more often than
  # that and doing it from every CI run was redundant, wasted API calls.
  cleanup-ci-images:
    name: Clean up staging and cache images older than 7 days
    runs-on: ubuntu-latest
    steps:
      - name: Delete old versions
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |  # shell
          # gha-runner-compose-staging and gha-runner-compose-cache are both
          # created by CI workflows using the default token, which auto-grants
          # this repo admin rights on them - unlike gha-runner-compose(-base),
          # which nothing but publish-release.yml ever writes to.
          #
          # Keep for 7 days: GHCR layer deduplication (content-addressable
          # blobs) means later pushes skip re-uploading layers already present
          # in the registry, even under a different tag. A content-tag that's
          # still being reused gets its updated_at refreshed on every reuse
          # (ci.yml/ci-large-tiers.yml re-push it, a cheap no-op for unchanged
          # content), so only tags nothing has referenced in 7+ days actually
          # get pruned here.
          CUTOFF=$(date -u -d '7 days ago' +%s 2>/dev/null || date -u -v-7d +%s)
          for PACKAGE in gha-runner-compose-staging gha-runner-compose-cache; do
            VERSION_IDS=$(gh api --paginate "/user/packages/container/${PACKAGE}/versions" \
              --jq ".[] | select((.updated_at | fromdateiso8601) < ${CUTOFF}) | .id" 2>/dev/null || true)
            if [ -z "${VERSION_IDS}" ]; then
              echo "No ${PACKAGE} versions older than 7 days to delete."
              continue
            fi
            for ID in ${VERSION_IDS}; do
              echo "Deleting ${PACKAGE} version ${ID} (older than 7 days)"
              gh api --method DELETE "/user/packages/container/${PACKAGE}/versions/${ID}" 2>/dev/null \
                || echo "  ⚠️ Failed to delete version ${ID}."
            done
          done

  update-runner-images-src:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: true

      - name: Config & Update submodule
        run: |  # shell
          # Config & Update submodule
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git submodule update --remote
          git add runner-images-src

      - name: Check for submodule changes
        id: check_changes
        run: |  # shell
          # Check for changes in the submodule
          echo "HEAD submodule commit: $(git rev-parse HEAD:runner-images-src)"
          echo "Index submodule commit: $(git rev-parse :runner-images-src)"
          set +e
          git diff --cached --quiet runner-images-src
          exit_code=$?
          set -e
          if [ $exit_code -eq 0 ]; then
            echo "No changes detected in submodule"
            echo "changes=false" >> $GITHUB_OUTPUT
          elif [ $exit_code -eq 1 ]; then
            echo "Changes detected in submodule"
            echo "changes=true" >> $GITHUB_OUTPUT
          else
            echo "Error checking submodule changes: exit code $exit_code" >&2
            exit $exit_code
          fi

      - name: Copy sources
        if: steps.check_changes.outputs.changes == 'true'
        id: copy_sources
        run: |  # shell
          # Copy Ubuntu scripts
          cp -r runner-images-src/images/ubuntu/scripts/* docker-assets/from-upstream/
          # Copy toolset
          cp runner-images-src/images/ubuntu/toolsets/toolset-2404.json docker-assets/from-upstream/toolset.json

          submodule_ref=$(git rev-parse --short :runner-images-src)
          echo "submodule_ref=$submodule_ref" >> $GITHUB_OUTPUT

          # Check if docker-assets/from-upstream has changes
          if git diff --quiet -- docker-assets/from-upstream; then
            echo "No changes detected in docker-assets/from-upstream: commit new sub-module ref: $submodule_ref"
            echo "modified=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected in docker-assets/from-upstream (ref: $submodule_ref): create a PR"
            echo "modified=true" >> $GITHUB_OUTPUT
          fi

      - name: Create or update pull request
        if: steps.copy_sources.outputs.modified == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: update-runner-images
          title: Update runner images sources
          body: Automated update of runner images sources from upstream.
          commit-message: Update runner images sources
          delete-branch: true

      - name: Commit and push submodule update
        if: steps.check_changes.outputs.changes == 'true' && steps.copy_sources.outputs.modified == 'false'
        run: |  # shell
          # Commit and push submodule update
          git add runner-images-src
          git commit -m "[auto] Update runner-images-src submodule to ${{ steps.copy_sources.outputs.submodule_ref }}"
          git push